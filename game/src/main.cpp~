// Created by Niffoxic (Harsh Dubey)
#include <engine/engine.h>

#include "spdlog/spdlog.h"

int main()
{
    cots::engine engine;
    if (not engine.init())
        return 1;
    return engine.execute();
}
